import { newId } from "./newId.js";
import { IdManager } from "./IdManager.js";

export const uuidManager = { newId, isUuidManager: true };

function unused_03284092349() {
    const typeTest: IdManager = uuidManager;
}
